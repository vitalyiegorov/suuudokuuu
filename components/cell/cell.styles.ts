import {StyleSheet} from "react-native";

export const CellStyles = StyleSheet.create({
    cell: { flex: 1, borderWidth: 1, borderStyle: 'solid', borderColor: 'black', alignItems: 'center', justifyContent: 'center' }
})