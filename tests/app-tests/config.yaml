flows:
  - "flows/*"

platform:
  ios:
    snapshotKeyHonorModalViews: false
    disableAnimations: true
  android:
    disableAnimations: true

onFlowStart:
  - startRecording: 'run-recording'
  - tapOn:
      text: '^Open$'
      optional: true
onFlowComplete:
  - stopRecording
