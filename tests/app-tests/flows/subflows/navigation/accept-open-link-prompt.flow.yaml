appId: ${APP_ID}
---
- extendedWaitUntil:
    visible: '^Open$'
    timeout: 10000
    optional: true
- runFlow:
    when:
      visible: '^Open$'
    commands:
      - tapOn: '^Open$'
      - waitForAnimationToEnd
