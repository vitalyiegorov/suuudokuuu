appId: ${APP_ID}
---
- openLink:
    link: ${DEV_CLIENT_LINK}
- runFlow: accept-open-link-prompt.flow.yaml
- extendedWaitUntil:
    visible:
      id: 'HomeScreenSelectors.StartButton'
    timeout: 60000
- openLink:
    link: suuudokuuu://
- runFlow: accept-open-link-prompt.flow.yaml
- extendedWaitUntil:
    visible:
      id: 'HomeScreenSelectors.StartButton'
    timeout: 60000
