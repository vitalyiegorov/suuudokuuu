import { StyleSheet } from 'react-native';

export const LoserStyles = StyleSheet.create({
    container: {
        alignItems: 'center',
        flex: 1,
        gap: 20,
        justifyContent: 'center'
    }
});
