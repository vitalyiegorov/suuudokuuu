import { homePageMetadata } from '../../app/metadata';

import type { PageMetadataInterface } from '../interfaces/page-metadata.interface';

export const PAGE_METADATA_REGISTRY: PageMetadataInterface[] = [homePageMetadata];
