import { createSlot } from '../../utils/create-slot.util';

import type { SlotChildrenPropsInterface } from '../../interfaces/slot-children-props.interface';
import type { SlotComponentType } from '../../types/slot-component.type';

export const SoftwareApplicationFeature: SlotComponentType<SlotChildrenPropsInterface> = createSlot();
