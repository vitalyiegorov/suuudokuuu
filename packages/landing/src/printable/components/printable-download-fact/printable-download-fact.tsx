import { createSlot } from '../../../seo/utils/create-slot.util';

import type { SlotChildrenPropsInterface } from '../../../seo/interfaces/slot-children-props.interface';
import type { SlotComponentType } from '../../../seo/types/slot-component.type';

export const PrintableDownloadFact: SlotComponentType<SlotChildrenPropsInterface> = createSlot();
