import type { CandidateNodeInterface } from '../interfaces/candidate-node.interface';

export const AIC_MAX_LINK_VISITS = 20_000;
export const AIC_MIN_NODES = 4;
export const EMPTY_TARGET_CELL_NODES: readonly CandidateNodeInterface[] = Object.freeze([]);
