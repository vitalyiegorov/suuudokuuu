import { StyleSheet } from 'react-native';

export const PrivacyPolicyScreenStyles = StyleSheet.create({
    container: {
        alignItems: 'center',
        flex: 1,
        padding: 20
    },
    scrollView: {
        marginBottom: 20
    }
});
