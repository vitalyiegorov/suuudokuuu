import { StyleSheet } from 'react-native';

export const PrivacyPolicyScreenStyles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20
    },
    scrollView: {
        paddingBottom: 20
    }
});
