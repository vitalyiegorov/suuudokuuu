import { StyleSheet } from 'react-native';

export const BetaInstallActionsStyles = StyleSheet.create({
    button: {
        flex: 1,
        minWidth: 220
    },
    container: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        gap: 12,
        width: '100%'
    }
});
