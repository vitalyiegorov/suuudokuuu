import { StyleSheet } from 'react-native';

export const ChallengeLostScreenStyles = StyleSheet.create({
    boldText: {
        fontWeight: 'bold'
    },
    container: {
        alignItems: 'center',
        flex: 1,
        gap: 20,
        justifyContent: 'center'
    },
    differenceText: {
        marginTop: 10
    },
    icon: {
        marginBottom: 10
    },
    messageText: {
        marginTop: 10
    },
    statsContainer: {
        alignItems: 'center',
        gap: 4
    }
});
