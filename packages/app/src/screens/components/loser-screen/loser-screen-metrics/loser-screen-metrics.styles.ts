import { StyleSheet } from 'react-native';

export const LoserScreenMetricsStyles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        gap: 12,
        width: '100%'
    }
});
