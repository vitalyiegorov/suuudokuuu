import { StyleSheet } from 'react-native';

export const LoserScreenActionsStyles = StyleSheet.create({
    primaryButton: {
        width: '100%'
    },
    secondaryButton: {
        width: '100%'
    }
});
