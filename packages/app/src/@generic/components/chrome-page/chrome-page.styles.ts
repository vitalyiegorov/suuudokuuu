import { StyleSheet } from 'react-native-unistyles';

export const ChromePageStyles = StyleSheet.create(() => ({
    container: {
        flex: 1
    },
    header: {
        left: 0,
        position: 'absolute',
        right: 0,
        top: 0,
        zIndex: 3
    }
}));
