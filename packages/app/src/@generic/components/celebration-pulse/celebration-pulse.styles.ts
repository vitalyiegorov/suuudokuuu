import { StyleSheet } from 'react-native';

export const CelebrationPulseStyles = StyleSheet.create({
    content: {
        alignItems: 'center',
        justifyContent: 'center'
    },
    ring: {
        borderRadius: 999,
        position: 'absolute'
    },
    wrap: {
        alignItems: 'center',
        justifyContent: 'center',
        position: 'relative'
    }
});
