import { StyleSheet } from 'react-native';

export const BlackSubHeaderStyles = StyleSheet.create({
    container: {
        fontFamily: 'Inter_700Bold',
        fontSize: 18,
        marginBottom: 10
    }
});
