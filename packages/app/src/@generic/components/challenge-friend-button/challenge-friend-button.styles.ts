import { StyleSheet } from 'react-native';

export const ChallengeFriendButtonStyles = StyleSheet.create({
    button: {
        flexDirection: 'row',
        gap: 8
    },
    icon: {
        marginRight: 4
    }
});
