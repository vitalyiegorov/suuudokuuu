import { StyleSheet } from 'react-native-unistyles';

export const GameResultMetricsStyles = StyleSheet.create(theme => ({
    metrics: {
        flexDirection: 'row',
        gap: theme.spacing.md,
        width: '100%'
    }
}));
