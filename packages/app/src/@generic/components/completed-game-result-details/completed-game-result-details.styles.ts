import { StyleSheet } from 'react-native-unistyles';

export const CompletedGameResultDetailsStyles = StyleSheet.create(theme => ({
    item: {
        gap: theme.spacing.xs,
        width: 96
    },
    strip: {
        alignSelf: 'center'
    }
}));
