import { StyleSheet } from 'react-native';

export const BlurGradientStyles = StyleSheet.create({
    bottom: {
        bottom: 0
    },
    container: {
        left: 0,
        position: 'absolute',
        right: 0,
        zIndex: 2
    },
    top: {
        top: 0
    }
});
