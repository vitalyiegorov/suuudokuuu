import { StyleSheet } from 'react-native';

export const AvailableValuesItemStyles = StyleSheet.create({
    progressRing: {
        bottom: 0,
        left: 0,
        position: 'absolute',
        right: 0,
        top: 0
    },
    text: {
        zIndex: 1
    }
});
