import { StyleSheet } from 'react-native';

export const ReplayFieldStyles = StyleSheet.create({
    cellText: {
        fontFamily: 'Inter_500Medium'
    },
    row: {
        flexDirection: 'row'
    },
    wrapper: {
        aspectRatio: 1
    }
});
