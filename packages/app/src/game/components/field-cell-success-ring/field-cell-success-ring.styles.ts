import { StyleSheet } from 'react-native';

export const FieldCellSuccessRingStyles = StyleSheet.create({
    ring: {
        borderRadius: 9,
        borderWidth: 2.5,
        bottom: 0,
        left: 0,
        position: 'absolute',
        right: 0,
        top: 0,
        zIndex: 2
    }
});
