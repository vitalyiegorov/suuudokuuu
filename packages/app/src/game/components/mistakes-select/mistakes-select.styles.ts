import { StyleSheet } from 'react-native';

export const MistakesSelectStyles = StyleSheet.create({
    hardcoreText: {
        fontWeight: 'bold'
    },
    wrapper: {
        marginBottom: 20,
        rowGap: 5
    }
});
