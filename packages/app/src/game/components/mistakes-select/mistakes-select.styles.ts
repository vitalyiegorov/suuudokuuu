import { StyleSheet } from 'react-native';

export const MistakesSelectStyles = StyleSheet.create({
    button: {
        minWidth: 150
    },
    hardcoreText: {
        fontWeight: 'bold'
    },
    wrapper: {
        alignItems: 'center',
        flex: 1,
        justifyContent: 'center',
        marginBottom: 20,
        rowGap: 5
    }
});
