import { StyleSheet } from 'react-native-unistyles';

export const UseKeyboardControlsStyles = StyleSheet.create(() => ({
    hiddenInput: {
        height: 0,
        position: 'absolute',
        width: 0
    }
}));
