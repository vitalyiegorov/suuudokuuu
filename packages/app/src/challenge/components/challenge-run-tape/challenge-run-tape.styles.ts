import { StyleSheet } from 'react-native-unistyles';

export const ChallengeRunTapeStyles = StyleSheet.create({
    container: {
        gap: 9,
        width: '100%'
    },
    header: {
        alignItems: 'center',
        flexDirection: 'row',
        gap: 10,
        justifyContent: 'space-between'
    },
    label: {
        fontFamily: 'Inter_600SemiBold',
        fontSize: 12.5,
        letterSpacing: 0.9,
        textTransform: 'uppercase'
    }
});
