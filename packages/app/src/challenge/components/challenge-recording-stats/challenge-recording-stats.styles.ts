import { StyleSheet } from 'react-native-unistyles';

export const ChallengeRecordingStatsStyles = StyleSheet.create(theme => ({
    row: {
        alignItems: 'center',
        flexDirection: 'row',
        flexWrap: 'wrap',
        gap: theme.spacing.sm,
        justifyContent: 'center',
        width: '100%'
    }
}));
