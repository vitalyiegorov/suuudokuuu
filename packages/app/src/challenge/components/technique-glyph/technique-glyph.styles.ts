import { StyleSheet } from 'react-native';

export const TechniqueGlyphStyles = StyleSheet.create({
    cell: {
        borderRadius: 1,
        flex: 1
    },
    row: {
        flex: 1,
        flexDirection: 'row'
    }
});
