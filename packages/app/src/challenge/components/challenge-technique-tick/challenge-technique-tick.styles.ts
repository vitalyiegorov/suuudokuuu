import { StyleSheet } from 'react-native';

export const ChallengeTechniqueTickStyles = StyleSheet.create({
    tick: {
        borderRadius: 2,
        bottom: 0,
        marginLeft: -1.5,
        position: 'absolute'
    }
});
