import { StyleSheet } from 'react-native-unistyles';

export const ChallengeRecordingSummaryStyles = StyleSheet.create(theme => ({
    container: {
        alignItems: 'stretch',
        gap: theme.spacing.md,
        width: '100%'
    }
}));
