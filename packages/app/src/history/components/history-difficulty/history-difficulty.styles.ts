import { StyleSheet } from 'react-native';

export const HistoryDifficultyStyles = StyleSheet.create({
    boldText: {
        fontWeight: 'bold'
    },
    container: {
        alignItems: 'center'
    }
});
