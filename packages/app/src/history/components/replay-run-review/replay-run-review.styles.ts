import { StyleSheet } from 'react-native-unistyles';

export const ReplayRunReviewStyles = StyleSheet.create(theme => ({
    container: {
        gap: theme.spacing.md,
        width: '100%'
    }
}));
