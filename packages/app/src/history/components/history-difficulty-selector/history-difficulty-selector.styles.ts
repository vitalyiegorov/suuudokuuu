import { StyleSheet } from 'react-native';

export const HistoryDifficultySelectorStyles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        gap: 9,
        width: '100%'
    }
});
