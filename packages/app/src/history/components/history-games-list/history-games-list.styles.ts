import { StyleSheet } from 'react-native';

export const HistoryGamesListStyles = StyleSheet.create({
    container: {
        gap: 12,
        width: '100%'
    },
    list: {
        gap: 10,
        width: '100%'
    }
});
