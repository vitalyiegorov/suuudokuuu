import { StyleSheet } from 'react-native';

export const ReplayTechniqueStyles = StyleSheet.create({
    container: {
        alignItems: 'center',
        minHeight: 18
    },
    text: {
        fontSize: 12,
        fontWeight: '600',
        maxWidth: 180,
        textAlign: 'center'
    }
});
