import { StyleSheet } from 'react-native';

export const CompletedGamesListStyles = StyleSheet.create({
    container: {
        marginTop: 8
    },
    emptyText: {
        textAlign: 'center',
        opacity: 0.6
    }
});
