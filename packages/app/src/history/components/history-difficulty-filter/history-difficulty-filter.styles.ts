import { StyleSheet } from 'react-native';

export const HistoryDifficultyFilterStyles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        gap: 8,
        width: '100%'
    }
});
