import { StyleSheet } from 'react-native-unistyles';

export const HistoryOverviewStyles = StyleSheet.create(() => ({
    container: {
        gap: 20,
        width: '100%'
    },
    difficultySection: {
        gap: 12,
        width: '100%'
    }
}));
