import { StyleSheet } from 'react-native-unistyles';

export const ReplayPaceMetricsStyles = StyleSheet.create(theme => ({
    container: {
        gap: theme.spacing.sm,
        width: '100%'
    },
    separator: {
        height: 28,
        marginHorizontal: 2
    },
    strip: {
        paddingHorizontal: 0,
        width: '100%'
    }
}));
