import { StyleSheet } from 'react-native-unistyles';

export const CompletedGameTechniqueSummaryStyles = StyleSheet.create(theme => ({
    row: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        gap: theme.spacing.xs,
        width: '100%'
    }
}));
