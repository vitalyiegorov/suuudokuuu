import { StyleSheet } from 'react-native-unistyles';

const ReplayShareActionButtonSize = 44;

export const ReplayShareActionStyles = StyleSheet.create({
    button: {
        borderRadius: ReplayShareActionButtonSize / 2,
        height: ReplayShareActionButtonSize,
        width: ReplayShareActionButtonSize
    }
});
