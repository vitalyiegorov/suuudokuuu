import { StyleSheet } from 'react-native';

export const ThemeListRowStyles = StyleSheet.create({
    pressable: { width: '100%' },
    trailing: { alignItems: 'center', flexDirection: 'row', gap: 8 }
});
