import { StyleSheet } from 'react-native';

export const SettingsSwitchStyles = StyleSheet.create({
    container: {
        alignItems: 'center',
        flexDirection: 'row',
        justifyContent: 'space-between'
    },
    switch: {}
});
