import { StyleSheet } from 'react-native-unistyles';

export const SettingsComfortModeSectionStyles = StyleSheet.create(theme => ({
    container: {
        borderCurve: 'continuous',
        borderRadius: theme.radius.lg,
        borderWidth: 1,
        padding: theme.spacing.md,
        width: '100%'
    }
}));
