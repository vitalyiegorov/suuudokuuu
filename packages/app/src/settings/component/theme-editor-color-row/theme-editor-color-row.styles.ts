import { StyleSheet } from 'react-native';

export const ThemeEditorColorRowStyles = StyleSheet.create({
    pressable: { width: '100%' },
    swatchFrame: { borderRadius: 6, borderWidth: 1, overflow: 'hidden' },
    swatch: { height: 28, width: 44 }
});
